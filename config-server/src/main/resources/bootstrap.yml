server:
  port: 8888
spring:
  application.name: config-server

spring.cloud.config.server.git.uri: file://${user.home}/Developer/spring/microservices-config

eureka:
  client:
    serviceUrl.defaultZone: http://localhost:8761/eureka/
    region: default
    registryFetchIntervalSeconds: 5
